CREATE TABLE IF NOT EXISTS song_genre
(
    song_genre_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    song_id      BIGINT REFERENCES song (song_id) ON DELETE CASCADE NOT NULL,
    genre_id      BIGINT REFERENCES genre (genre_id) ON DELETE CASCADE NOT NULL
);